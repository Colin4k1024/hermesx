services:
  postgres-test:
    image: postgres:16
    container_name: hermes-pg-test
    environment:
      POSTGRES_DB: hermes_test
      POSTGRES_USER: hermes_test
      POSTGRES_PASSWORD: hermes_test
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hermes_test"]
      interval: 3s
      timeout: 3s
      retries: 10
    tmpfs:
      - /var/lib/postgresql/data

  redis-test:
    image: redis:7
    container_name: hermes-redis-test
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10

  minio-test:
    image: minio/minio:latest
    container_name: hermes-minio-test
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: hermes_test
      MINIO_ROOT_PASSWORD: hermes_test
    ports:
      - "9002:9000"
      - "9003:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 3s
      timeout: 3s
      retries: 10
    tmpfs:
      - /data
